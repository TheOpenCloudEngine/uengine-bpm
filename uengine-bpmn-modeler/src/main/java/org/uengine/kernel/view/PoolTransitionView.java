package org.uengine.kernel.view;

public class PoolTransitionView /*extends TransitionView*/ {
/*	
	PoolTransition poolTransition;
	public PoolTransition getPoolTransition() {
		return poolTransition;
	}
	public void setPoolTransition(PoolTransition poolTransition) {
		this.poolTransition = poolTransition;
	}

@ServiceMethod(callByContent=true, target="popup")
public ModalWindow poolMapping() throws Exception{
	PoolMappingPanel poolMappingPanel = new PoolMappingPanel();
	poolMappingPanel.setMappingId(this.getId());
	poolMappingPanel.setMappingName(poolTransition.getTransitionName() == null ? "" : poolTransition.getTransitionName());
	poolMappingPanel.setParentEditorId(this.getEditorId());
	poolMappingPanel.setPoolTransition(getPoolTransition());
	poolMappingPanel.getMetaworksContext().setWhen(MetaworksContext.WHEN_EDIT);
	poolMappingPanel.load();
    
    ModalWindow popup = new ModalWindow();
    popup.setId(this.getId());				// 꼭 필요함
	popup.setTitle("데이터 매핑");
	popup.setPanel(poolMappingPanel);
	popup.setWidth(1000);
	popup.setHeight(600);
    
    return popup;
}
	*/
}

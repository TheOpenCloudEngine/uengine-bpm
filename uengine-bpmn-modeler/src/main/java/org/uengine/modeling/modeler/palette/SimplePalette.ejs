	<table width="100%">
	<% if(mw3.where != "popUp"){
	%>
		<tr>
			<td style="padding:10px; background-color:#bbbbbb;color:#ffffff;font-size:12pt;border-bottom:0px;" colspan="2">
				<%=value.name%>
			</td>
		</tr>
	<%
	}
	%>
</table>

	<div id="objDiv_<%=objectId%>_contents">
	<table width="100%">
	<%
		if(typeof options == 'undefined')
			options = objectMetadata;

		options['htmlTag'] =  'tr';

		for(var i=0; i<value.symbolList.length; i++){
	%>
			<%=mw3.locateObject(value.symbolList[i], value.symbolList[i].__className, null, options, {objectId: value.__objectId, name: 'symbolList[' + i + ']'})%>
	<%
		}
	%>
	</table>
	</div>